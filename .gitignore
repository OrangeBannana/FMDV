# Build intermediates / outputs (binaries ship via GitHub Releases, not git)
*.o
*.syso
fmdv_res.o
fmdv*.exe

# Headless test render outputs (but keep README screenshots)
*.png
!docs/*.png

# Local scratch builds for manual testing (not part of the repo)
/test-builds/

# CLI/core build output (portable Makefile + CMake) and raw benchmark logs
# (only the bench/results summary markdown is committed, not raw CSVs)
/build/
/build-cmake/
bench/results/*.csv

# OS / OneDrive cruft
Thumbs.db
desktop.ini
*.tmp

# Local agent session config
/.claude/

# OS / macOS cruft
.DS_Store

# Local build toolchains / wine sandbox (cross-compilation & verification rig)
.tools/

# IDE / editor local config + scratch
.vscode/
.idea/
*.swp
*.swo
~*
*.bak
*.log

# Wine sandbox prefix (kept out of the repo if ever placed at workspace root)
winehome/
